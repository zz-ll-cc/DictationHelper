<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dictation.mapper.BackMapper">
    <select id="findAll" resultType="com.dictation.back.entity.Back">
        select * from tbl_back
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="baid">
        insert into tbl_back(time,info,uid,type,status,path)
            values(#{time},#{info},#{uid},#{type},#{status},#{path})
    </insert>





</mapper>